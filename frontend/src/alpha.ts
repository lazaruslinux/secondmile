// The whole alpha marker, in one file so the day it ends is a deletion rather
// than a hunt: remove this module and the handful of imports the compiler then
// points at. Nothing else in the app knows what alpha means.
//
// The notice is the one the landing page has always shown, said in the same
// words to somebody already signed in: what is promised is still being built.
export const ALPHA = true

export const ALPHA_NOTICE =
  'This project is currently in closed Alpha testing and is in active development. Features may change, evolve, or go away entirely. A lot of the artwork is temporary. You may experience bugs and sync issues. Please use the bug report feature, and have fun.'
